#include <iostream>
using namespace std;
}#include <iostream>
#include <stack>
using namespace std;

int main() {
    string s = "{[()]}";
    stack<char> st;

    for (char c : s) {

    cout << (st.empty() ? "Valid" : "Invalid");
    return 0;
}#include <iostream
int main() {

    for (int i = 0; i < s.size(); i++
    cout << "Integer: " << ans;
    return 0;
}
